<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug only. This file overlays the release one by build type and never ships.

  The operator console's API runs locally over plain HTTP during development —
  packages/api has a docker-compose and no TLS — and the emulator reaches the
  host machine at 10.0.2.2. So cleartext is permitted TO THOSE THREE NAMES AND
  NO OTHERS: the base config below still refuses it for every other host, which
  means a developer who points a debug build at a real staging server still gets
  HTTPS enforced rather than a silent downgrade.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
